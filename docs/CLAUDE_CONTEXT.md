# CLAUDE CONTEXT

This project was originally a single massive HTML file generated by AI.

The project has been reorganized to reduce context overload in Claude Projects.

Guidelines:
- Edit only relevant component files when possible
- Avoid inlining large assets again
- Keep CSS modular
- Preserve responsiveness and visual hierarchy
- Maintain existing branding and styling direction

Preferred workflow:
- Analyze one section at a time
- Avoid loading the entire project context unnecessarily
